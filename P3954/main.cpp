// luogu p3954

#include <iostream>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    int a = 0, b = 0, c = 0;
    cin >> a >> b >> c;
    double total = a * 0.2 + b * 0.3 + c * 0.5;
    cout << total << endl;
    return 0;
}
