// luogu b2034

#include <iostream>
#include <cmath>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    int n{};
    cin >> n;
    long output{(long) pow(2, n)};
    cout << output << endl;
    return 0;
}
