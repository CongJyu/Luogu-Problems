// luogu p2006

#include <iostream>
#include <vector>

using namespace std;

struct Skill {
    int a;
    int b;
    int number;
};

int main() {
    ios::sync_with_stdio(false);
    int k{}, m{}, n{};
    cin >> k >> m >> n;
    while (m--) {
        int tmp_a{}, tmp_b{};
        cin >> tmp_a >> tmp_b;
        Skill tmp_skill{tmp_a, tmp_b};
        
    }
    return 0;
}
